create table zipcodes (
    country varchar(15)
    zipcode varchar(15),
    city varchar(500),
    state varchar(100),
    latitude varchar(15),
    longitude varchar(15),
    classification_code varchar(5),
    population varchar(50)
)